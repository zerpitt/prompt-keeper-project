# Firebase Configuration
# Copy the JSON config object from Firebase Console and minify it (or just paste the values if you change the parsing logic, but current logic expects JSON string for VITE_FIREBASE_CONFIG)
# Example: VITE_FIREBASE_CONFIG={"apiKey":"...","authDomain":"...","projectId":"..."}
VITE_FIREBASE_CONFIG=

# Application ID (Optional, defaults to 'default-app-id')
VITE_APP_ID=prompt-keeper-v1
